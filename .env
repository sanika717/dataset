# Root .env — only JWT_SECRET_KEY is read from here by docker-compose.yml
# (the DATABASE_URL for the containerized backend is set directly in
# docker-compose.yml, pointing at the `db` service).
JWT_SECRET_KEY=MySiteSafeSecretKey2026!@#-
DATABASE_URL=postgresql://sanik@localhost:5432/sitesafe